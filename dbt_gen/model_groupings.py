from global_logger import GLOBAL_LOGGER
import os

class ModelGroupings:
    
    def __init__(repo_root):
    ## loop through the folders directly in {{repo_root}}/models,
    ## return an array of folder names
        pass
