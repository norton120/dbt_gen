
from os import * 
from tkinter import * as tk

class initial_config(self):
    '''
        creates the config file:
            - gets the DBT repo path from the user.
            - gets the snowflake creds.
            TODO: instruct user to set permissions on the config file! 
            - creates the dbt_gen file tree
            - returns status 
    '''    

    def request_repo_location():
                
